---
phase: 02-cdn-data-plane
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-03
files_modified:
  - counterpick-app/apps/backend/backend.py
  - counterpick-app/apps/backend/requirements.txt
  - counterpick-app/apps/backend/backend.spec
  - .github/workflows/build-smoke.yml
  - counterpick-app/apps/backend/test_json_repo_contract.py
  - counterpick-app/apps/backend/pyproject.toml
autonomous: true
requirements:
  - CDN-08
tags:
  - python
  - cutover
  - atomic-commit
  - backend-import-swap
  - pyinstaller-excludes
  - ci-guard
  - contract-tests

must_haves:
  truths:
    - "backend.py no longer imports from lolalytics_api.supabase_repo / supabase_client / config on the runtime path — the import block at lines 11-21 now pulls from lolalytics_api.json_repo."
    - "requirements.txt no longer lists supabase>=2.4.0 on the runtime line; supabase_repo.py stays in source for contract tests only."
    - "backend.spec excludes block contains supabase, gotrue, postgrest, realtime, storage3, supabase_functions, supabase_auth (restored from Phase 1 deferral per commit 451e8f7)."
    - "build-smoke.yml 'Verify supabase NOT in bundle' step is uncommented and enforces strings | grep -i supabase returns empty."
    - "pyinstaller --clean --noconfirm backend.spec produces a .exe that does NOT contain supabase strings, and the smoke test round-trips end-to-end successfully."
    - "test_json_repo_contract.py contains @pytest.mark.contract tests that, when run with -m contract and live Supabase + live CDN creds, assert json_repo.get_X(...) == supabase_repo.get_X(...) for all 9 public functions."
    - "Contract tests are gated: they skip cleanly when SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY env vars are absent (pitfall #7 mitigation)."
    - "The cutover lands as ONE atomic commit touching all five files in lockstep — intermediate states would break CI (Pitfall #3, commit 451e8f7 is the precedent)."
  artifacts:
    - path: "counterpick-app/apps/backend/backend.py"
      provides: "Modified imports: supabase_repo/supabase_client/config lines REPLACED with json_repo; json_repo.warm_cache() called at startup in main()"
      contains: "from lolalytics_api.json_repo import (get_champion_stats as json_get_champion_stats, ...)"
    - path: "counterpick-app/apps/backend/requirements.txt"
      provides: "Runtime deps minus supabase — supabase>=2.4.0 line removed"
      contains: "requests\\n...\\nplatformdirs>=4.0.0 (and NO supabase line)"
    - path: "counterpick-app/apps/backend/backend.spec"
      provides: "PyInstaller spec with supabase family excludes restored from Phase 1 commit 451e8f7"
      contains: "excludes=[... 'supabase', 'gotrue', 'postgrest', 'realtime', 'storage3', 'supabase_functions', 'supabase_auth', 'eventlet', 'gevent', 'tkinter']"
    - path: ".github/workflows/build-smoke.yml"
      provides: "Re-enabled 'Verify supabase NOT in bundle' CI guard"
      contains: "Verify supabase NOT in bundle ... strings dist/backend-x86_64-pc-windows-msvc.exe | grep -i supabase"
    - path: "counterpick-app/apps/backend/test_json_repo_contract.py"
      provides: "Live-credential parity tests — one per public function — gated by @pytest.mark.contract and env-var skipif"
      contains: "pytestmark = [pytest.mark.contract, pytest.mark.skipif(...)]; test_get_items_parity, test_get_runes_parity, test_get_summoner_spells_parity, test_get_champion_stats_parity, test_get_champion_stats_by_role_parity, test_get_matchups_parity, test_get_synergies_parity"
    - path: "counterpick-app/apps/backend/pyproject.toml"
      provides: "pytest markers config registering the contract marker so pytest -m contract works cleanly"
      contains: "[tool.pytest.ini_options] markers = ['contract: live-credential parity test']"
  key_links:
    - from: "backend.py startup (main())"
      to: "json_repo.warm_cache()"
      via: "module-level import + explicit call before socketio.run"
      pattern: "json_repo\\.warm_cache\\(\\)"
    - from: "backend.py route handlers"
      to: "json_repo.get_* functions"
      via: "rebinding sb_get_* aliases to json_get_*"
      pattern: "from lolalytics_api\\.json_repo import"
    - from: "build-smoke.yml CI guard"
      to: "dist/backend-x86_64-pc-windows-msvc.exe"
      via: "strings | grep -i supabase"
      pattern: "strings dist/.*backend-x86_64-pc-windows-msvc\\.exe \\| grep -i supabase"
    - from: "test_json_repo_contract.py"
      to: "supabase_repo (still in source) AND json_repo (via live CDN)"
      via: "deep-equal comparison on return values"
      pattern: "_assert_rows_equal\\("
---

# OBJECTIVE

Execute the Phase 2 atomic cutover: flip `backend.py` from `supabase_repo` to `json_repo`, remove `supabase` from the runtime deps, restore the supabase family excludes in `backend.spec`, re-enable the "Verify supabase NOT in bundle" CI guard, and add live-credential contract parity tests. **All of this MUST land in ONE atomic commit** — any intermediate state breaks CI (Pitfall #3; commit `451e8f7` is the precedent — flipping any single one of these in isolation broke the frozen `.exe`).

**Critical invariant #1 from orchestrator:** ATOMIC. Single commit. All five load-bearing files. No intermediate states. Do NOT split into per-file commits.

**Dependencies:**
- Plan 02-01 (`json_repo.py` landed, all unit tests pass).
- Plan 02-03 (Task 3 operator checkpoint — CDN is LIVE and reachable at `https://chertixd.github.io/LoL-Draft-Helper/data/`). If the CDN is not live when this plan runs, `backend.py` startup will fail loud with `CDNError` (per D-19 unreachable-CDN + no-cache policy).

**Critical invariant #5 from orchestrator:** Contract tests require live Supabase AND live CDN creds — they MUST be gated behind `@pytest.mark.contract` + env var skipif so they skip cleanly in `build-smoke.yml` (which lacks Supabase keys) but run deliberately in the daily ETL workflow (which has the keys and the data is fresh).

**Post-Wave-3 state:** Phase 2 goal achieved.
- Installed `.exe` does not contain `supabase` strings — verified by the restored CI guard.
- Client performs zero Supabase calls at runtime — all reads go through the CDN.
- Contract tests in CI prove json_repo stays behaviorally equivalent to supabase_repo over time.
- Phase 3 (Tauri shell) can now consume a stable backend contract.

# EXECUTION CONTEXT

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

# CONTEXT

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cdn-data-plane/02-CONTEXT.md
@.planning/phases/02-cdn-data-plane/02-RESEARCH.md
@.planning/phases/02-cdn-data-plane/02-01-PLAN.md
@.planning/phases/02-cdn-data-plane/02-03-PLAN.md
@.planning/phases/01-sidecar-foundation/01-CONTEXT.md

## Files being modified (MUST be read before editing)

@counterpick-app/apps/backend/backend.py
@counterpick-app/apps/backend/requirements.txt
@counterpick-app/apps/backend/backend.spec
@.github/workflows/build-smoke.yml

## The contract being tested

@counterpick-app/apps/backend/src/lolalytics_api/supabase_repo.py
@counterpick-app/apps/backend/src/lolalytics_api/json_repo.py

## The SUMMARY recording Phase 1's deferred supabase fight

@.planning/phases/01-sidecar-foundation/01-02-SUMMARY.md

## Interfaces

The import block in `backend.py` lines 11-21 (verified from file read):

```
from lolalytics_api.supabase_repo import (
    get_champion_stats as sb_get_champion_stats,
    get_champion_stats_by_role as sb_get_champion_stats_by_role,
    get_items as sb_get_items,
    get_runes as sb_get_runes,
    get_summoner_spells as sb_get_summoner_spells,
    get_matchups as sb_get_matchups,
    get_synergies as sb_get_synergies,
)
from lolalytics_api.supabase_client import get_supabase_client
from lolalytics_api.config import get_supabase_url
```

The replacement import block (exact shape — copy the 7 `get_*` aliases and drop the two client/config lines):

```
from lolalytics_api.json_repo import (
    get_champion_stats as sb_get_champion_stats,
    get_champion_stats_by_role as sb_get_champion_stats_by_role,
    get_items as sb_get_items,
    get_runes as sb_get_runes,
    get_summoner_spells as sb_get_summoner_spells,
    get_matchups as sb_get_matchups,
    get_synergies as sb_get_synergies,
    warm_cache as _json_repo_warm_cache,
    stale_status as _json_repo_stale_status,
)
```

Notice: the `sb_` alias prefix is PRESERVED (not renamed to `json_`) per D-22 — "kept if semantic". This keeps every route-handler call site unchanged. Only the import source swaps.

The two REMOVED lines (`supabase_client`, `config.get_supabase_url`) are gone because `json_repo` does not need either.

`warm_cache` gets called ONCE at backend startup, in `main()`, AFTER argparse + logging setup but BEFORE socketio.run. See Task 1 action for exact placement.

# TASKS

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land test_json_repo_contract.py + pytest marker config (RED-skip phase — test file lands, skips in CI without live creds)</name>
  <files>
    counterpick-app/apps/backend/test_json_repo_contract.py
    counterpick-app/apps/backend/pyproject.toml
  </files>
  <read_first>
    - counterpick-app/apps/backend/src/lolalytics_api/supabase_repo.py (all 444 lines — the contract shape)
    - counterpick-app/apps/backend/src/lolalytics_api/json_repo.py (Plan 02-01 output — the CDN-backed mirror)
    - counterpick-app/apps/backend/pyproject.toml (current state — pytest config section, if any)
    - .planning/phases/02-cdn-data-plane/02-RESEARCH.md Example 3 (contract test skeleton, lines 902-996)
    - .planning/phases/02-cdn-data-plane/02-RESEARCH.md Pitfall #7 (live-credential leakage + skipif pattern)
    - .planning/phases/02-cdn-data-plane/02-CONTEXT.md D-29 (contract tests are daily CI, not per-commit)
  </read_first>
  <behavior>
    **pyproject.toml update:** add `[tool.pytest.ini_options]` if absent, with a `markers` list containing `"contract: live-credential parity test (skipped by default; daily-CI only)"`. Do not touch any other pyproject.toml sections. If pyproject.toml already has `[tool.pytest.ini_options]`, add only the `markers` key (or append to an existing markers list).

    **test_json_repo_contract.py structure:**

    1. Module docstring citing CDN-01.

    2. Imports:
       ```
       from __future__ import annotations
       import os
       import pytest
       from lolalytics_api import supabase_repo, json_repo
       ```

    3. Module-level gate (Pitfall #7):
       ```
       pytestmark = [
           pytest.mark.contract,
           pytest.mark.skipif(
               not (os.environ.get("SUPABASE_URL") and os.environ.get("SUPABASE_SERVICE_ROLE_KEY")),
               reason="contract test needs live Supabase credentials",
           ),
           pytest.mark.skipif(
               not os.environ.get("CDN_CONTRACT_TEST_ENABLE"),
               reason="contract test opt-in; set CDN_CONTRACT_TEST_ENABLE=1",
           ),
       ]
       ```
       The double skip (creds + opt-in env var) ensures the tests never surprise-run in local development or on PR CI — only in the daily cron that explicitly sets both.

    4. Helpers (copy from RESEARCH Example 3 lines 930-941):
       - `_stable_sort(rows, keys)` — deterministic sort for order-insensitive compare.
       - `_assert_rows_equal(a, b, keys)` — deep-equal modulo ordering with diagnostic on mismatch.

    5. Seven test functions (one per public function per D-29 + D-11), using live data:
       - `test_get_items_parity()` — `supabase_repo.get_items()` vs `json_repo.get_items()`; sort keys `("patch", "item_id")` (or whatever the actual item primary key is — if `item_id` doesn't exist, use whatever `supabase_repo.get_items` rows expose as a stable key).
       - `test_get_runes_parity()` — same pattern; sort keys `("patch", "rune_id")` or equivalent.
       - `test_get_summoner_spells_parity()` — sort keys `("patch", "spell_key")` or equivalent.
       - `test_get_champion_stats_parity()` — takes a real champion ("Aatrox"); asserts `a == b` on the resulting single dict (not a list — this function returns a dict per the interfaces block).
       - `test_get_champion_stats_by_role_parity()` — takes "Ahri"; asserts `a == b` on the result dict (contains `statsByRole`).
       - `test_get_matchups_parity()` — "Lux" in middle; compare top-level scalars `base_winrate`, `base_wilson`, `role` (pytest.approx on floats); `_assert_rows_equal(a["by_delta"], b["by_delta"], keys=("opponent_key",))` and same for `by_normalized`.
       - `test_get_synergies_parity()` — "Lulu" in support; compare `base_winrate` (pytest.approx), `role`; `_assert_rows_equal(a["rows"], b["rows"], keys=("mate_key",))`.

    **Stable-sort key discovery** (research assumption A3): the test requires a primary-key tuple per row-bearing table. If the research-suggested tuples don't work (e.g., `item_id` doesn't exist as a column), discover the actual PK at runtime:
    - `keys = tuple(sorted(supabase_repo.get_items()[0].keys()))[:3]` as a fallback (all keys, sort-stable, first 3 — deterministic enough for tests); better: prefer `("patch", "item_id")`, fall back to `("patch", "key")` if `item_id` isn't in the row.

    **Expected runtime behavior:**
    - Without `CDN_CONTRACT_TEST_ENABLE=1` + creds set: all 7 tests skip with clear reason messages. `pytest counterpick-app/apps/backend/` — 7 skipped.
    - On daily CI with creds + opt-in: all 7 tests run and pass (provided the exporter and json_repo agree, which is the whole point of the test).

    Since this test will skip cleanly in the build-smoke environment (no creds), it is safe to land as part of the atomic cutover commit — no special staging.
  </behavior>
  <action>
    1. Open `counterpick-app/apps/backend/pyproject.toml`. If `[tool.pytest.ini_options]` does not exist, append:
       ```
       [tool.pytest.ini_options]
       markers = [
           "contract: live-credential parity test (skipped by default; set CDN_CONTRACT_TEST_ENABLE=1 plus SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY to run)",
       ]
       ```
       If it exists, inspect and merge only the `markers` key. Preserve everything else byte-for-byte.

    2. Create `counterpick-app/apps/backend/test_json_repo_contract.py` per the behavior spec above. Use RESEARCH Example 3 as the direct template; extend with the two-gate skipif pattern and the primary-key-discovery fallback.

    3. Verify the skip behavior locally:
       ```
       cd counterpick-app/apps/backend
       pytest test_json_repo_contract.py -v
       # Expected: 7 skipped with reason "contract test opt-in" or "needs live Supabase credentials".
       ```
       If any test ATTEMPTS to run (because a cred was set in your local env unexpectedly), the skipif is misconfigured — fix it before proceeding.

    4. Do NOT commit yet. This task's file creations become part of the single atomic commit Task 5 produces. Keep the working tree dirty.
  </action>
  <verify>
    <automated>cd counterpick-app/apps/backend &amp;&amp; pytest test_json_repo_contract.py -v 2>&amp;1 | grep -E "(7 skipped|skipped.*contract)"</automated>
  </verify>
  <acceptance_criteria>
    - `counterpick-app/apps/backend/test_json_repo_contract.py` exists on disk.
    - File contains `pytestmark = [` + `pytest.mark.contract` + two `pytest.mark.skipif(` entries (one for creds, one for opt-in env var `CDN_CONTRACT_TEST_ENABLE`).
    - File imports both `from lolalytics_api import supabase_repo, json_repo`.
    - File contains `def test_get_items_parity(`, `def test_get_runes_parity(`, `def test_get_summoner_spells_parity(`, `def test_get_champion_stats_parity(`, `def test_get_champion_stats_by_role_parity(`, `def test_get_matchups_parity(`, `def test_get_synergies_parity(` — seven tests total.
    - File contains `_assert_rows_equal(` helper.
    - `counterpick-app/apps/backend/pyproject.toml` contains `[tool.pytest.ini_options]` with a `markers` list registering the `contract` marker.
    - `pytest test_json_repo_contract.py -v` reports all 7 as SKIPPED (not errored) when creds + `CDN_CONTRACT_TEST_ENABLE` are absent.
    - **NO git commit yet** — working tree stays dirty for the atomic cutover (Task 5).
  </acceptance_criteria>
  <done>
    Contract test file + marker config staged on disk. In a no-creds environment (local dev, build-smoke CI), all 7 tests skip cleanly. In the daily ETL workflow with both creds and opt-in, the tests will run against live data. Working tree stays dirty; atomic commit lands in Task 5.
  </done>
</task>

<task type="auto">
  <name>Task 2: Edit backend.py — swap supabase_repo import block to json_repo + wire warm_cache at startup</name>
  <files>counterpick-app/apps/backend/backend.py</files>
  <read_first>
    - counterpick-app/apps/backend/backend.py (ALL of it — lines 1-60 for imports, search for main() to see the startup flow from Plan 01-02, search for `sb_get_` to enumerate call sites that must remain unchanged)
    - counterpick-app/apps/backend/src/lolalytics_api/json_repo.py (Plan 02-01 output — exact function names exported)
    - .planning/phases/02-cdn-data-plane/02-CONTEXT.md D-22 (import swap, sb_* alias preservation), D-19 (unreachable-CDN policy = fail loud here, Phase 3 wraps UX)
    - .planning/phases/01-sidecar-foundation/01-02-SUMMARY.md (the main() structure from Plan 01-02 — probe thread ordering, logging setup)
    - .planning/phases/02-cdn-data-plane/02-RESEARCH.md Pitfall #3 (cutover ordering — why this MUST land atomically with Tasks 3 + 4 + 5)
  </read_first>
  <behavior>
    Three edits to `backend.py`:

    **Edit A — Import block swap (lines ~11-21, exact block verified from file read):**

    Remove the existing 3-statement block:
    ```
    from lolalytics_api.supabase_repo import (
        get_champion_stats as sb_get_champion_stats,
        ... 7 aliases total ...
    )
    from lolalytics_api.supabase_client import get_supabase_client
    from lolalytics_api.config import get_supabase_url
    ```

    Replace with:
    ```
    # Phase 2 CDN-02..08: json_repo replaces supabase_repo on the runtime path.
    # The sb_* alias prefix is retained (D-22) so every downstream call site
    # (/api/tierlist, /api/matchups, etc.) is unchanged. supabase_client +
    # config imports are DROPPED — json_repo needs neither.
    from lolalytics_api.json_repo import (
        get_champion_stats as sb_get_champion_stats,
        get_champion_stats_by_role as sb_get_champion_stats_by_role,
        get_items as sb_get_items,
        get_runes as sb_get_runes,
        get_summoner_spells as sb_get_summoner_spells,
        get_matchups as sb_get_matchups,
        get_synergies as sb_get_synergies,
        warm_cache as _json_repo_warm_cache,
        stale_status as _json_repo_stale_status,
        CDNError as _json_repo_CDNError,
    )
    ```

    **Edit B — Wire warm_cache() into main():**

    Find `def main()` in backend.py (it was added by Plan 01-02 — see 01-02-SUMMARY.md). Its current shape (from the summary):
    ```
    def main() -> None:
        # 1. Parse args (argparse)
        # 2. Configure logging
        # 3. setdefault SSL_CERT_FILE / REQUESTS_CA_BUNDLE
        # 4. Unlink stale ready-file
        # 5. Start daemon probe thread
        # 6. Print dev-mode banner
        # 7. socketio.run(...)
    ```

    Insert a warm_cache call between steps 4 and 5 (AFTER stale-file cleanup, BEFORE the probe thread starts — so warm_cache runs before the ready-file ever appears, guaranteeing Tauri sees the backend only once it has data):

    ```
        # Phase 2: warm the CDN cache before declaring ready. CDNError propagates
        # to os._exit via the probe-timeout fallback if fetching fails; Phase 3's
        # UX layer will replace this with a friendly error banner (first-run
        # offline UX is intentionally deferred per D-19 + N-05).
        try:
            _json_repo_warm_cache()
        except _json_repo_CDNError as exc:
            logging.getLogger(__name__).error("[json_repo] warm_cache failed: %s", exc)
            # Fail loud — do NOT write ready-file. Tauri will time out the probe
            # and show the Phase 3 AV-troubleshooting dialog (TAURI-07).
            import os as _os
            _os.write(2, f"[json_repo] warm_cache failed: {exc}\n".encode())
            _os.replace  # touch to ensure the import is used (no lint warning)
            _os._exit(2)
    ```

    The double-underscore import (`_json_repo_CDNError`) mirrors how `backend.py` already handles `_probe_health_then_signal_ready` — prefix indicates "module-private, do not expose".

    **Edit C — Add /api/health 'cached' field per D-19:**

    Find the existing `/api/health` route handler (augmented by Plan 01-02 with the `version` field per the summary). Add a `"cached"` key to the response body populated from `_json_repo_stale_status()`:

    Locate the existing return:
    ```
    return jsonify({
        "status": "ok",
        "version": "1.0.0-dev",
        "service": "lolalytics-backend",
        "cache_entries": len(...),
        "timestamp": datetime.now(timezone.utc).isoformat(),
    })
    ```

    Insert before the `return`:
    ```
        _cached = _json_repo_stale_status()
    ```

    and add a key to the dict:
    ```
        "cached": _cached,   # D-19: {table: stale_bool} for Phase 3 UX staleness indicator
    ```

    This doesn't change any existing field; it adds one new field. Plan 03 (Phase 3 Tauri + UX) consumes it; Plan 01's test_backend_cli.py only asserts `status` and `version` so it remains green.

    **Negative-guard checks before saving:**
    ```
    grep -n 'from lolalytics_api.supabase_repo' counterpick-app/apps/backend/backend.py   # expect 0
    grep -n 'from lolalytics_api.supabase_client' counterpick-app/apps/backend/backend.py # expect 0
    grep -n 'from lolalytics_api.config' counterpick-app/apps/backend/backend.py          # expect 0
    grep -n 'from lolalytics_api.json_repo' counterpick-app/apps/backend/backend.py       # expect 1
    grep -n 'warm_cache' counterpick-app/apps/backend/backend.py                           # expect >= 2 (import + call site)
    ```

    **Call sites unchanged:** every `sb_get_champion_stats(...)`, `sb_get_matchups(...)`, etc. must work with zero edits because the alias prefix is preserved. Verify:
    ```
    grep -c 'sb_get_' counterpick-app/apps/backend/backend.py
    ```
    Should return the same count before and after the edit.

    **Do NOT commit yet.** Working tree stays dirty for Task 5.
  </behavior>
  <action>
    Open `counterpick-app/apps/backend/backend.py`. Apply the three edits above (A, B, C) using the Edit tool — one Edit call per edit to keep diffs reviewable:

    1. Edit A: find the 11-line supabase import block (from `from lolalytics_api.supabase_repo import (` through `from lolalytics_api.config import get_supabase_url`); replace with the 13-line json_repo block. The `sb_*` alias names MUST match supabase_repo's existing aliases exactly (verify via grep before editing).

    2. Edit B: find `def main()`. Locate the step between "unlink stale ready-file" and "start daemon probe thread" per the Plan 01-02 summary. Insert the try/except warm_cache block. Use `_os._exit(2)` (not `sys.exit`) because we are on the main thread here (pre-socketio.run) — either works, but os._exit is the pattern Plan 01-02 established for the probe-timeout case.

    3. Edit C: find `@app.route('/api/health')` and the existing return statement. Insert `_cached = _json_repo_stale_status()` and add `"cached": _cached` to the returned dict.

    After applying all three edits, run the negative-guard greps and confirm each matches expectations. Run `python -c "import ast; ast.parse(open('counterpick-app/apps/backend/backend.py').read())"` to verify syntax.

    Do NOT commit. Atomic commit lands in Task 5.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('counterpick-app/apps/backend/backend.py').read())" &amp;&amp; ! grep -n 'from lolalytics_api.supabase_repo' counterpick-app/apps/backend/backend.py &amp;&amp; ! grep -n 'from lolalytics_api.supabase_client' counterpick-app/apps/backend/backend.py &amp;&amp; ! grep -n 'from lolalytics_api.config' counterpick-app/apps/backend/backend.py &amp;&amp; grep -q 'from lolalytics_api.json_repo' counterpick-app/apps/backend/backend.py &amp;&amp; grep -q '_json_repo_warm_cache' counterpick-app/apps/backend/backend.py</automated>
  </verify>
  <acceptance_criteria>
    - `backend.py` syntax valid (`ast.parse` succeeds).
    - `grep 'from lolalytics_api.supabase_repo' backend.py` returns 0 hits.
    - `grep 'from lolalytics_api.supabase_client' backend.py` returns 0 hits.
    - `grep 'from lolalytics_api.config' backend.py` returns 0 hits.
    - `grep 'from lolalytics_api.json_repo' backend.py` returns 1 hit (the new import block).
    - `grep 'warm_cache as _json_repo_warm_cache' backend.py` returns 1 hit.
    - `grep 'stale_status as _json_repo_stale_status' backend.py` returns 1 hit.
    - `grep 'CDNError as _json_repo_CDNError' backend.py` returns 1 hit.
    - `grep '_json_repo_warm_cache()' backend.py` returns 1 hit (the call site in main()).
    - `grep '_json_repo_stale_status()' backend.py` returns 1 hit (the /api/health call site).
    - `grep -c 'sb_get_' backend.py` is the same value as before the edit (call sites unchanged).
    - No git commit yet.
  </acceptance_criteria>
  <done>
    backend.py imports json_repo, calls warm_cache at startup, exposes /api/health.cached. All downstream `sb_get_*` call sites work unchanged. Syntax valid. Working tree dirty.
  </done>
</task>

<task type="auto">
  <name>Task 3: Edit requirements.txt + backend.spec — drop supabase from runtime deps + restore PyInstaller excludes</name>
  <files>
    counterpick-app/apps/backend/requirements.txt
    counterpick-app/apps/backend/backend.spec
  </files>
  <read_first>
    - counterpick-app/apps/backend/requirements.txt (current file — 12 lines, supabase at line 10)
    - counterpick-app/apps/backend/backend.spec (current file — excludes block is commented out around lines 64-82 per Phase 1 commit 451e8f7 note)
    - .planning/phases/02-cdn-data-plane/02-CONTEXT.md D-23 (requirements.txt supabase removal), D-24 (spec excludes restoration)
    - .planning/phases/01-sidecar-foundation/01-01-SUMMARY.md (the original backend.spec decisions)
    - Git log entry for 451e8f7 — the Phase 1 commit that deferred this excludes restoration
  </read_first>
  <behavior>
    **Edit A — requirements.txt:** remove the line `supabase>=2.4.0`. The current file (verified from read):
    ```
    requests
    lxml
    pytest
    urllib3
    flask
    flask-cors
    flask-socketio>=5.3.0
    python-socketio>=5.9.0
    websocket-client>=1.6.0
    supabase>=2.4.0     <-- DELETE THIS LINE
    python-dotenv
    platformdirs>=4.0.0
    ```

    After edit, the file has 11 lines. `grep '^supabase' requirements.txt` returns 0 hits.

    **Edit B — backend.spec:** restore the supabase family excludes. The current excludes block (verified from read, lines 64-83):

    ```
    excludes=[
        # NOTE: Phase 1 local-test discovery (2026-04-14): CONTEXT D-09 tried to
        # exclude the supabase family here, but backend.py still imports
        # `from lolalytics_api.supabase_repo import ...` at module load (per
        # N-03, the full cutover to json_repo.py is Phase 2 work). Excluding
        # supabase made the frozen .exe crash at startup with
        # ModuleNotFoundError. Excludes are re-added in Phase 2 when the
        # supabase_repo import is replaced by json_repo.
        #
        # Phase 2 will restore:
        #     'supabase', 'gotrue', 'postgrest', 'realtime', 'storage3',
        #     'supabase_functions', 'supabase_auth',
        #
        # Locked async_mode='threading' — exclude the alternatives so PyInstaller
        # doesn't waste cycles trying to bundle them.
        'eventlet',
        'gevent',
        # Heavy stdlib leaves we never use:
        'tkinter',
    ],
    ```

    Rewrite to:

    ```
    excludes=[
        # Phase 2 CDN-08 (commit <THIS-COMMIT-HASH>, 2026-04-14): restored from the
        # Phase 1 deferral (commit 451e8f7). json_repo has now replaced
        # supabase_repo on the runtime path, so the supabase family is safe to
        # exclude — the bundle shrinks ~5 MB.
        'supabase',
        'gotrue',
        'postgrest',
        'realtime',
        'storage3',
        'supabase_functions',
        'supabase_auth',
        # Locked async_mode='threading' — exclude the alternatives so PyInstaller
        # doesn't waste cycles trying to bundle them.
        'eventlet',
        'gevent',
        # Heavy stdlib leaves we never use:
        'tkinter',
    ],
    ```

    (The `<THIS-COMMIT-HASH>` is a placeholder — after the atomic commit lands in Task 5, update it via a follow-up comment edit. Skip this in the initial write.)

    **Negative-guard:** `grep '^\s*#.*supabase' counterpick-app/apps/backend/backend.spec` should still return 0 hits of the deferral note (i.e., the commented-out `'supabase'` entry is gone, replaced by an active `'supabase'` string in the list).

    **Positive guard:** `grep "'supabase'" counterpick-app/apps/backend/backend.spec` returns 1 hit (the active exclude).

    **Do NOT commit yet.** Working tree stays dirty for Task 5.
  </behavior>
  <action>
    1. Open `counterpick-app/apps/backend/requirements.txt`. Delete the line `supabase>=2.4.0` (use Edit tool with `old_string="supabase>=2.4.0\n"` and `new_string=""`). Verify the line count dropped from 12 to 11 with `wc -l`.

    2. Open `counterpick-app/apps/backend/backend.spec`. Locate the excludes block (~lines 64-83). Use the Edit tool to replace the entire commented-out block + active `'eventlet', 'gevent', 'tkinter'` entries with the new block that has supabase family excludes active.

    Preserve the opening `excludes=[` line and closing `],` exactly.

    3. Run syntax checks:
       ```
       python -c "import ast; ast.parse(open('counterpick-app/apps/backend/backend.spec').read())"
       ```
       (backend.spec is valid Python; PyInstaller parses it as a Python file.)

    4. Run negative/positive guards:
       ```
       ! grep '^supabase' counterpick-app/apps/backend/requirements.txt
       grep "'supabase'" counterpick-app/apps/backend/backend.spec | wc -l   # expect >= 1
       ! grep -E "^#.*Phase 2 will restore" counterpick-app/apps/backend/backend.spec
       ```

    5. Do NOT commit. Atomic commit lands in Task 5.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('counterpick-app/apps/backend/backend.spec').read())" &amp;&amp; ! grep -q '^supabase' counterpick-app/apps/backend/requirements.txt &amp;&amp; grep -q "'supabase'" counterpick-app/apps/backend/backend.spec &amp;&amp; grep -q "'gotrue'" counterpick-app/apps/backend/backend.spec &amp;&amp; grep -q "'postgrest'" counterpick-app/apps/backend/backend.spec    </automated>
  </verify>
  <acceptance_criteria>
    - `counterpick-app/apps/backend/requirements.txt` has exactly 11 non-empty lines, NO `supabase` line.
    - `counterpick-app/apps/backend/backend.spec` contains active (not commented) `'supabase'`, `'gotrue'`, `'postgrest'`, `'realtime'`, `'storage3'`, `'supabase_functions'`, `'supabase_auth'` entries in the `excludes=[...]` list.
    - `backend.spec` still contains `'eventlet'`, `'gevent'`, `'tkinter'` excludes (preserved).
    - `backend.spec` NO LONGER contains the Phase 1 deferral comment (no `Phase 2 will restore` string).
    - `python -c "import ast; ast.parse(open('counterpick-app/apps/backend/backend.spec').read())"` exits 0.
    - No git commit yet.
  </acceptance_criteria>
  <done>
    supabase removed from runtime deps; PyInstaller excludes restored. Bundle size will drop ~5 MB once the spec is rebuilt. Working tree stays dirty for atomic commit.
  </done>
</task>

<task type="auto">
  <name>Task 4: Edit build-smoke.yml — re-enable the Verify-supabase-NOT-in-bundle CI guard</name>
  <files>.github/workflows/build-smoke.yml</files>
  <read_first>
    - .github/workflows/build-smoke.yml (ALL lines — specifically the commented-out guard block at lines 66-82)
    - .planning/phases/02-cdn-data-plane/02-CONTEXT.md D-25
    - .planning/phases/01-sidecar-foundation/01-03-SUMMARY.md (the Plan 01-03 summary recording this deferral, if present)
  </read_first>
  <behavior>
    The existing commented-out guard block in `build-smoke.yml` (lines 66-82, verified from file read) begins with `# NOTE: Phase 1 local-test discovery` and ends after the `#       fi` line.

    Rewrite as an active step — strip the `# ` prefix from the 12 `#   - name:` through `#       fi` lines; preserve indent; replace the NOTE-block preamble with a brief active-step comment. The final active block:

    ```yaml
      # Phase 2 CDN-08: strings guard — dist/backend-x86_64-pc-windows-msvc.exe
      # MUST NOT contain any supabase-family substrings after the import swap
      # (D-22) + requirements.txt removal (D-23) + backend.spec excludes (D-24).
      # Runs AFTER the PyInstaller build step, BEFORE the smoke_test_exe step.
      - name: Verify supabase NOT in bundle
        working-directory: counterpick-app/apps/backend
        shell: bash
        run: |
          command -v strings >/dev/null 2>&1 || { echo "::error::strings binary not available on runner" >&2; exit 1; }
          if strings dist/backend-x86_64-pc-windows-msvc.exe | grep -i supabase; then
            echo "::error::supabase strings leaked into the bundle"
            exit 1
          fi
    ```

    **Placement note:** the guard runs AFTER `pyinstaller --clean --noconfirm backend.spec` (line 64) and BEFORE `Smoke test built .exe` (line 84). The current commented block is already in the right position; uncommenting preserves order.

    **Note on `strings` availability on windows-latest:** `strings` ships with Git-for-Windows bash (which `shell: bash` resolves to on windows-latest). The explicit `command -v strings` gate makes this failure visible with a clear error message instead of a cryptic exit code.

    Do NOT commit yet. Working tree stays dirty for Task 5.
  </behavior>
  <action>
    Use the Edit tool on `.github/workflows/build-smoke.yml` to replace the entire commented-out NOTE block (lines 66-82 inclusive) with the active guard step per the behavior spec. Preserve the exact 6-space step-list indentation.

    Verify YAML parses: `python -c "import yaml; yaml.safe_load(open('.github/workflows/build-smoke.yml'))"`.

    Grep guards:
    - `grep -c 'Verify supabase NOT in bundle' .github/workflows/build-smoke.yml` returns 1.
    - `grep -c 'Phase 1 local-test discovery' .github/workflows/build-smoke.yml` returns 0.
    - `grep -c '^\s*#.*strings dist/' .github/workflows/build-smoke.yml` returns 0 (no commented-out variant remains).

    Do NOT commit. Atomic commit lands in Task 5.
  </action>
  <verify>
    <automated>
    python -c "import yaml; yaml.safe_load(open('.github/workflows/build-smoke.yml'))" && grep -q 'Verify supabase NOT in bundle' .github/workflows/build-smoke.yml && ! grep -q '^\s*#.*strings dist/' .github/workflows/build-smoke.yml
    </automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/build-smoke.yml` parses as valid YAML.
    - File contains active step `Verify supabase NOT in bundle` (not commented out).
    - File does NOT contain `Phase 2 will restore` or `Re-enable in Phase 2` comments.
    - File does NOT contain `#       if strings dist/` (the commented-out variant must be gone).
    - `strings dist/backend-x86_64-pc-windows-msvc.exe | grep -i supabase` pattern appears ONCE in the file, on an active `run: |` line.
    - No git commit yet.
  </acceptance_criteria>
  <done>
    CI guard re-enabled; any future regression that reintroduces supabase into the bundle will fail build-smoke with a clear "supabase strings leaked into the bundle" error. Working tree dirty, awaiting atomic commit.
  </done>
</task>

<task type="auto">
  <name>Task 5: Atomic commit + local frozen-build smoke test + push</name>
  <files>
    (no new file edits — this task stages + commits + builds + verifies the Tasks 1-4 dirty tree)
  </files>
  <read_first>
    - counterpick-app/apps/backend/backend.py (verify Task 2 edits present)
    - counterpick-app/apps/backend/requirements.txt (verify Task 3 edit A present)
    - counterpick-app/apps/backend/backend.spec (verify Task 3 edit B present)
    - .github/workflows/build-smoke.yml (verify Task 4 edit present)
    - counterpick-app/apps/backend/test_json_repo_contract.py (verify Task 1 present)
    - counterpick-app/apps/backend/pyproject.toml (verify Task 1 marker registration)
    - .planning/phases/02-cdn-data-plane/02-RESEARCH.md Pitfall #3 (cutover ordering — commit 451e8f7 lesson)
  </read_first>
  <behavior>
    Commit all the changes from Tasks 1-4 as ONE atomic git commit. This is the orchestrator's critical invariant #1 — any intermediate state breaks CI; a single commit ensures CI sees the whole shape at once.

    **Pre-commit checks (ALL must pass before staging):**

    1. `git status` — working tree shows exactly these files modified/added:
       - counterpick-app/apps/backend/backend.py
       - counterpick-app/apps/backend/requirements.txt
       - counterpick-app/apps/backend/backend.spec
       - .github/workflows/build-smoke.yml
       - counterpick-app/apps/backend/test_json_repo_contract.py (new)
       - counterpick-app/apps/backend/pyproject.toml
       No other files. If other files are dirty, investigate and clean up before committing.

    2. Run all the grep-based guard assertions from Tasks 2-4.

    3. Local dev-mode smoke test — prove json_repo + backend.py module-load without supabase:

       ```
       cd counterpick-app/apps/backend
       pip uninstall -y supabase gotrue postgrest realtime storage3 supabase_functions supabase_auth
       python -c "from lolalytics_api import json_repo; print(json_repo.CDN_BASE_URL)"
       python -c "import importlib.util; spec = importlib.util.spec_from_file_location('backend', 'backend.py'); m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m); print('backend.py module-load OK')"
       ```

       Both commands MUST succeed. This proves the atomic cutover broke the supabase chain. Note: after Task 3 requirements.txt no longer pins supabase, so a fresh `pip install -r requirements.txt` also won't bring it back.

    4. Local frozen-build smoke test (the real proof):

       ```
       cd counterpick-app/apps/backend
       pip install pyinstaller==6.19.0
       pyinstaller --clean --noconfirm backend.spec
       strings dist/backend-x86_64-pc-windows-msvc.exe | grep -i supabase
       # ^ must return ZERO lines
       ```

       If `strings | grep` returns any lines, the excludes aren't working — investigate before committing.

    5. Run the full local test suite:

       ```
       pytest counterpick-app/apps/backend/ -v
       ```

       Expected: test_backend_cli passes (Plan 01-03), test_json_repo_cache passes 8/8 (Plan 02-01), test_json_repo_contract SKIPS 7/7 (no creds + no opt-in).

    **Atomic commit:**

    Stage all 6 files. Confirm `git status` shows ONLY those 6 staged. Create the commit with this message (via HEREDOC):

    ```
    feat(02-04): atomic CDN cutover -- swap supabase_repo to json_repo on runtime

    Phase 2 atomic cutover (CDN-08). All five load-bearing changes land in
    one commit because any intermediate state breaks CI (commit 451e8f7 is
    the precedent -- flipping any one of these in isolation made the
    frozen .exe crash).

    Changes:
    - backend.py: import block swapped from supabase_repo + supabase_client
      + config to json_repo. sb_* aliases preserved per D-22; no
      route-handler call site edits. warm_cache() wired into main() between
      stale-file cleanup and probe-thread start. /api/health exposes
      cached: {table: bool} for Phase 3 UX staleness (D-19).
    - requirements.txt: supabase>=2.4.0 line removed (D-23). supabase_repo.py
      stays in source for contract tests (D-29).
    - backend.spec: 7 supabase-family excludes restored (D-24). Bundle drops
      ~5 MB. Phase 1 commit 451e8f7 deferral comment removed.
    - build-smoke.yml: Verify supabase NOT in bundle CI guard re-enabled
      (D-25). Uses strings grep -i supabase on the built .exe.
    - test_json_repo_contract.py: new file (D-29). One test per public
      function asserting deep-equal between supabase_repo and json_repo.
      Gated by @pytest.mark.contract + dual skipif per Pitfall #7.
    - pyproject.toml: [tool.pytest.ini_options] registers the contract marker.

    Requirements: CDN-08.
    ```

    **Post-commit:**
    - `git push origin HEAD`.
    - Watch CI via `gh run watch -R Chertixd/LoL-Draft-Helper` (or GitHub UI).

    Expected CI outcome: build-smoke green. Specifically:
    - Enforce upx=False: pass (unchanged).
    - Unit + dev-mode integration tests: pass (test_backend_cli + test_json_repo_cache).
    - PyInstaller build: pass (exe built cleanly).
    - **Verify supabase NOT in bundle: pass.** ← Phase 2 success gate.
    - Smoke test built .exe: pass (Socket.IO round-trip + HTTPS CDN fetch via json_repo).

    If ANY CI step fails, do NOT create a follow-up commit. Amend (`git commit --amend`) with the fix and force-push. Keep the cutover atomic.
  </behavior>
  <action>
    1. Run pre-commit grep battery for all 6 files (Tasks 1-4 acceptance_criteria).
    2. Dev-mode smoke: uninstall supabase, import json_repo + backend.py, confirm no errors.
    3. Local PyInstaller build + `strings` guard; confirm empty grep.
    4. `pytest counterpick-app/apps/backend/ -v` — confirm expected pass/skip distribution.
    5. Stage the 6 files via `git add <each-file>`.
    6. Confirm `git status` shows ONLY the 6 staged.
    7. Commit via HEREDOC with the message above.
    8. `git push origin HEAD`.
    9. Watch CI; on green, done. On red, amend + force-push.
  </action>
  <verify>
    <automated>
    cd counterpick-app/apps/backend && pyinstaller --clean --noconfirm backend.spec && strings dist/backend-x86_64-pc-windows-msvc.exe | grep -i supabase | wc -l | tr -d ' ' | grep -q '^0$'
    </automated>
  </verify>
  <acceptance_criteria>
    - Exactly ONE git commit added on top of the pre-plan-02-04 HEAD, with message prefix `feat(02-04): atomic CDN cutover`.
    - That commit touches exactly 6 files: backend.py, requirements.txt, backend.spec, build-smoke.yml, test_json_repo_contract.py, pyproject.toml.
    - `git diff HEAD~1 HEAD --stat` shows all 6 files in one diffstat (no earlier commits split the changes).
    - Local `pyinstaller --clean --noconfirm backend.spec` succeeds.
    - `strings dist/backend-x86_64-pc-windows-msvc.exe | grep -i supabase | wc -l` returns 0.
    - `pytest counterpick-app/apps/backend/ -v` — test_backend_cli pass, test_json_repo_cache 8/8 pass, test_json_repo_contract 7/7 skipped.
    - After `git push`, CI run of build-smoke.yml on the pushed SHA passes all steps including the re-enabled `Verify supabase NOT in bundle`.
    - `curl -s https://chertixd.github.io/LoL-Draft-Helper/data/champion_stats.json` returns 200 with valid envelope (CDN remains live from Plan 02-03 Task 3).
  </acceptance_criteria>
  <done>
    Atomic cutover committed, pushed, CI green. Phase 2 goal achieved.
  </done>
</task>

</tasks>

# THREAT MODEL

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend.py runtime imports | No Supabase URL or key may reach this boundary after the swap. |
| Frozen .exe module graph | `strings` grep is the compile-time gate; any transitive import of `supabase` fails CI. |
| Contract test env | Live Supabase creds + opt-in env var together gate execution; default is skip. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-31 | T (Tampering) | PR that re-introduces `from lolalytics_api.supabase_repo` in backend.py | mitigate | `strings grep -i supabase` CI guard catches this at build-smoke; reviewers see the loud CI failure. |
| T-02-32 | I (Info disclosure) | Contract test leaks Supabase creds via workflow logs in a forked PR | mitigate | Dual skipif: creds missing AND opt-in env var missing → skip. Forked PRs have no secrets by default. Worst case: tests skip. |
| T-02-33 | D (Denial of service) | CDN unreachable on backend startup | accept | warm_cache raises CDNError → main() os._exit(2) → Tauri probe times out → Phase 3 UX shows the AV-troubleshooting dialog (TAURI-07). No crash to user. |
| T-02-34 | E (Elevation of privilege) | Partial/split commits re-create the 451e8f7 hazard | mitigate | Task 5 enforces single atomic commit; CI runs against the merged shape, not against intermediate states. |
| T-02-35 | T (Tampering) | json_repo sha256 canonical form drifts from export_to_json | mitigate | Plan 02-02 `test_sha256_canonical_form_matches_json_repo` + the contract tests in this plan catch the regression from two angles (unit + live). |
| T-02-36 | S (Spoofing) | Stale Supabase import left in a test file accidentally imports via sys.path | accept | `test_json_repo_contract.py` intentionally imports both supabase_repo and json_repo — that is the point of the test. No other runtime codepath imports supabase. |

# VERIFICATION

Phase-level success gate:

1. **Single atomic commit:** `git log --oneline -1 | grep 'feat(02-04): atomic CDN cutover'` succeeds. `git show --stat HEAD` shows exactly 6 files.
2. **No supabase in bundle:** local rebuild `pyinstaller --clean --noconfirm backend.spec` + `strings dist/backend-x86_64-pc-windows-msvc.exe | grep -i supabase` returns 0 lines. CI runs this on every push.
3. **No supabase in runtime imports:** `! grep -E 'from lolalytics_api\.(supabase_repo|supabase_client|config)' counterpick-app/apps/backend/backend.py`.
4. **json_repo is the runtime data source:** `grep 'from lolalytics_api.json_repo' counterpick-app/apps/backend/backend.py` returns 1 hit.
5. **Runtime deps minimal:** `! grep '^supabase' counterpick-app/apps/backend/requirements.txt`.
6. **Spec excludes restored:** `grep -c "'supabase'" counterpick-app/apps/backend/backend.spec` returns >= 1 (active, not commented).
7. **CI guard active:** `grep 'Verify supabase NOT in bundle' .github/workflows/build-smoke.yml` returns 1 hit on an active (not commented) step.
8. **Cache unit tests pass:** `pytest counterpick-app/apps/backend/test_json_repo_cache.py -v` → 8/8 passing.
9. **Contract tests skip cleanly in no-creds env:** `pytest counterpick-app/apps/backend/test_json_repo_contract.py -v` → 7/7 skipped with the opt-in / creds reason.
10. **Frozen bundle smoke test:** `build-smoke.yml` run on the pushed commit is green, including `Verify supabase NOT in bundle` and `Smoke test built .exe`.
11. **CDN still reachable:** `curl -sI https://chertixd.github.io/LoL-Draft-Helper/data/champion_stats.json` returns 200 (must remain live — Plan 02-03 Task 3 gated this).
12. **Backend starts cleanly in dev mode:** `python backend.py --port 5088 --ready-file /tmp/ready-02-04.json` writes the ready file within ~2 s (including the warm_cache fan-out), and `curl http://127.0.0.1:5088/api/health` returns `{"status":"ok","version":"1.0.0-dev","cached":{...},...}`.

# SUCCESS CRITERIA

Phase 2 is complete when:

1. Phase 2 goal line from ROADMAP.md is TRUE: "The Supabase read path is replaced end-to-end with a public GitHub-Pages CDN plus a conditional-GET local cache, and the ETL publishes fresh JSON daily without any client-side Supabase credential."
2. All 8 CDN-XX requirements covered:
   - CDN-01: json_repo mirrors 9 public functions (Plan 02-01, verified by contract tests here).
   - CDN-02: cache at `user_cache_dir()/cdn/<table>.json` + `<table>.meta.json` (Plan 02-01).
   - CDN-03: conditional GET with If-None-Match / If-Modified-Since (Plan 02-01).
   - CDN-04: corrupt-cache auto-delete + refetch (Plan 02-01).
   - CDN-05: export_to_json.py with `__meta` envelope + sha256 (Plan 02-02).
   - CDN-06: ETL workflow runs daily (Plan 02-03).
   - CDN-07: GitHub Pages serves stable URL, baked into json_repo (Plan 02-01 URL + Plan 02-03 operator checkpoint).
   - CDN-08: supabase removed from runtime (this plan — atomic cutover).
3. Atomic commit landed; CI green including the re-enabled guard.
4. Contract tests skipping locally, opted-in on daily CI.
5. Phase 1 commit 451e8f7 deferral fully resolved — the TODO comment block in backend.spec + build-smoke.yml is gone.
6. Phase 3 (Tauri shell) can start: its Python sidecar contract is frozen at Plan 01-02's `--port` / `--ready-file` CLI + Plan 02-04's CDN-backed data plane + Plan 02-04's `/api/health.cached` staleness surface.

# OUTPUT

After completion, create `.planning/phases/02-cdn-data-plane/02-04-SUMMARY.md`. Include:

- The atomic commit SHA.
- `git show --stat HEAD` output proving the 6-file shape.
- Build-smoke CI run ID + status (link).
- Local `strings | grep -i supabase` output (must be empty).
- Bundle size before vs. after (expect ~5 MB reduction).
- Resolution of Phase 1 commit 451e8f7 deferral (flip status: "fully resolved 2026-04-14 via this commit").
- Phase 2 completion declaration: all CDN-01..08 requirements TRUE; hand off to Phase 3.
